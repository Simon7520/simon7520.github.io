<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>单元测试框架Jest搭配TypeScript的安装与配置</title>
    <url>/posts/7984b310/</url>
    <content><![CDATA[<p>为项目安装并配置Jest单元测试环境</p>
<h1 id="分步指南"><a href="#分步指南" class="headerlink" title="分步指南"></a>分步指南</h1><p>传送门：<a href="https://jestjs.io/docs/getting-started">Jest - 快速入门</a></p>
<h2 id="1-安装jest："><a href="#1-安装jest：" class="headerlink" title="1. 安装jest："></a>1. 安装jest：</h2><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">npm i jest ts-jest @types/jest -D<br></code></pre></td></tr></table></figure>
<h2 id="2-初始化："><a href="#2-初始化：" class="headerlink" title="2. 初始化："></a>2. 初始化：</h2><p>npx jest —init<br>按照图中所示选择<br><img src="9bb199f2aa35579c5f27a52af34849a5.png" alt="jest --init"></p>
<p>tip:</p>
<pre><code>- *no;不使用ts，使用jest.config.js作为jest的配置文件；*
- *jsdom;使用jsdom作为测试环境（jsdom:一个类似浏览器的环境，项目是运行在浏览器的，需要在浏览器dom环境下测试）；*
- *yes;是否添加测试报告；*
- *babel;使用babel提供的测试报告（官网说明v8仍处于试验阶段，需node14以上使用，效果未知，不稳定，因此选用babel）；*
</code></pre><h2 id="3-安装jsdom环境："><a href="#3-安装jsdom环境：" class="headerlink" title="3. 安装jsdom环境："></a>3. 安装jsdom环境：</h2><ul>
<li><em>jest 28及以上版本不再内置jsdom插件，需要单独安装</em></li>
<li><em>安装官方eslint插件：</em><figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">npm i jest-environment-jsdom eslint-plugin-jest eslint-import-resolver-typescript jest-canvas-mock -D<br></code></pre></td></tr></table></figure>
<h2 id="4-创建test目录"><a href="#4-创建test目录" class="headerlink" title="4. 创建test目录"></a>4. 创建test目录</h2>在项目根目录下创建test目录，然后在test下创建<strong>mocks和</strong>tests__目录，创建.eslintrc.js和tsconfig.json文件<br><img src="9aa30da4eb5b38c96a92d3d765ef8bd2.png" alt="目录结构示例"><blockquote>
<p>附：配置示例</p>
<p><em><code>jest.config.js</code></em></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-comment">/*</span><br><span class="hljs-comment"> * For a detailed explanation regarding each configuration property, visit:</span><br><span class="hljs-comment"> * https://jestjs.io/docs/configuration</span><br><span class="hljs-comment"> */</span><br><br><span class="hljs-keyword">const</span> &#123; pathsToModuleNameMapper &#125; = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;ts-jest&#x27;</span>);<br><span class="hljs-keyword">const</span> &#123; compilerOptions &#125; = <span class="hljs-built_in">require</span>(<span class="hljs-string">&#x27;./tsconfig.json&#x27;</span>);<br><br><span class="hljs-variable language_">module</span>.<span class="hljs-property">exports</span> = &#123;<br>  <span class="hljs-comment">// A preset that is used as a base for Jest&#x27;s configuration</span><br>  <span class="hljs-attr">preset</span>: <span class="hljs-string">&#x27;ts-jest&#x27;</span>,<br>  <span class="hljs-comment">// The test environment that will be used for testing</span><br>  <span class="hljs-attr">testEnvironment</span>: <span class="hljs-string">&#x27;jsdom&#x27;</span>,<br>  <span class="hljs-comment">// The paths to modules that run some code to configure or set up the testing environment before each test</span><br>  <span class="hljs-attr">setupFiles</span>: [<span class="hljs-string">&#x27;jest-canvas-mock&#x27;</span>],<br>  <span class="hljs-comment">// Automatically clear mock calls, instances, contexts and results before every test</span><br>  <span class="hljs-attr">clearMocks</span>: <span class="hljs-literal">true</span>,<br>  <span class="hljs-comment">// Indicates whether the coverage information should be collected while executing the test</span><br>  <span class="hljs-attr">collectCoverage</span>: <span class="hljs-literal">true</span>,<br>  <span class="hljs-comment">// The directory where Jest should output its coverage files</span><br>  <span class="hljs-attr">coverageDirectory</span>: <span class="hljs-string">&#x27;test/coverage&#x27;</span>,<br>  <span class="hljs-comment">// The root directory that Jest should scan for tests and modules within</span><br>  <span class="hljs-comment">// rootDir: undefined,</span><br>  <span class="hljs-comment">// rootDir: __dirname,</span><br>  <span class="hljs-comment">// An array of file extensions your modules use</span><br>  <span class="hljs-attr">moduleFileExtensions</span>: [<span class="hljs-string">&#x27;ts&#x27;</span>, <span class="hljs-string">&#x27;js&#x27;</span>],<br>  <span class="hljs-comment">// A map from regular expressions to module names or to arrays of module names that allow to stub out resources with a single module</span><br>  <span class="hljs-attr">moduleNameMapper</span>: <span class="hljs-title function_">pathsToModuleNameMapper</span>(compilerOptions.<span class="hljs-property">paths</span>, &#123;<br>    <span class="hljs-attr">prefix</span>: <span class="hljs-string">&#x27;&lt;rootDir&gt;/&#x27;</span>,<br>  &#125;),<br>  <span class="hljs-comment">// The glob patterns Jest uses to detect test files</span><br>  <span class="hljs-attr">testMatch</span>: [<span class="hljs-string">&#x27;&lt;rootDir&gt;/test/__tests__/**/*.test.ts&#x27;</span>],<br>  <span class="hljs-comment">// A map from regular expressions to paths to transformers</span><br>  <span class="hljs-attr">transform</span>: &#123;<br>    <span class="hljs-string">&#x27;^.+\\.ts$&#x27;</span>: <span class="hljs-string">&#x27;ts-jest&#x27;</span>,<br>  &#125;,<br>&#125;;<br></code></pre></td></tr></table></figure>
<p><em>配置测试用例的eslint规则 <code>/test/.eslintrc.js</code></em></p>
<figure class="highlight js"><table><tr><td class="code"><pre><code class="hljs js"><span class="hljs-variable language_">module</span>.<span class="hljs-property">exports</span> = &#123;<br>  <span class="hljs-attr">extends</span>: [<span class="hljs-string">&#x27;../.eslintrc.js&#x27;</span>],<br>  <span class="hljs-attr">settings</span>: &#123;<br>    <span class="hljs-string">&#x27;import/resolver&#x27;</span>: &#123;<br>      <span class="hljs-attr">typescript</span>: &#123;<br>        <span class="hljs-attr">alwaysTryTypes</span>: <span class="hljs-literal">true</span>, <span class="hljs-comment">// always try to resolve types under `&lt;root&gt;@types` directory even it doesn&#x27;t contain any source code, like `@types/unist`</span><br><br>        <span class="hljs-comment">// Choose from one of the &quot;project&quot; configs below or omit to use &lt;root&gt;/tsconfig.json by default</span><br>        <span class="hljs-comment">// use &lt;root&gt;/path/to/folder/tsconfig.json</span><br>        <span class="hljs-attr">project</span>: <span class="hljs-string">&#x27;tsconfig.json&#x27;</span>,<br>      &#125;,<br>    &#125;,<br>  &#125;,<br>  <span class="hljs-attr">plugins</span>: [<span class="hljs-string">&#x27;jest&#x27;</span>],<br>  <span class="hljs-attr">overrides</span>: [<br>    <span class="hljs-comment">// unit-tests</span><br>    &#123;<br>      <span class="hljs-attr">files</span>: [<span class="hljs-string">&#x27;**/__tests__/**&#x27;</span>],<br>      <span class="hljs-attr">rules</span>: &#123;<br>        <span class="hljs-string">&#x27;jest/no-disabled-tests&#x27;</span>: <span class="hljs-string">&#x27;warn&#x27;</span>,<br>        <span class="hljs-string">&#x27;jest/no-focused-tests&#x27;</span>: <span class="hljs-string">&#x27;error&#x27;</span>,<br>        <span class="hljs-string">&#x27;jest/no-identical-title&#x27;</span>: <span class="hljs-string">&#x27;error&#x27;</span>,<br>        <span class="hljs-string">&#x27;jest/prefer-to-have-length&#x27;</span>: <span class="hljs-string">&#x27;warn&#x27;</span>,<br>        <span class="hljs-string">&#x27;jest/valid-expect&#x27;</span>: <span class="hljs-string">&#x27;error&#x27;</span>,<br>      &#125;,<br>    &#125;,<br>  ],<br>&#125;;<br><br></code></pre></td></tr></table></figure>
<p><em>代码覆盖率报告无需加入git版本管理，将<code>test/coverage</code>目录追加至<code>.gitignore</code></em></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><code class="hljs bash">...<br><br><span class="hljs-comment"># Unit test / coverage reports</span><br><span class="hljs-built_in">test</span>/coverage<br></code></pre></td></tr></table></figure>
<p><em><code>tsconfig.json</code>中配置路径别名</em></p>
<h2 id="5-愉快地开始单元测试："><a href="#5-愉快地开始单元测试：" class="headerlink" title="5. 愉快地开始单元测试："></a>5. 愉快地开始单元测试：</h2><p>在 <code>test/__test__</code> 目录下新增自己模块的单元测试目录及文件，开始单元测试代码编写<br>文件命名规范： <code>*.test.ts</code></p>
</blockquote>
</li>
</ul>
<h2 id="6-总结-踩坑记录："><a href="#6-总结-踩坑记录：" class="headerlink" title="6. 总结 - 踩坑记录："></a>6. 总结 - 踩坑记录：</h2><ul>
<li>默认preset为<code>babel-jest</code>，由于 <code>Babel</code> 对 <code>Typescript</code> 的支持是通过代码转换（Transpilation）实现的，而 Jest 在运行时并不会对你的测试用例做类型检查。 因此建议安装ts-jest来开启此功能</li>
<li>主要是在配置<code>tsconfig.json</code>路径别名时花费了大量时间，处理ts的报错以及eslint的报错问题；</li>
<li>以上配置的路径别名只需在<code>tsconfig.json</code>一处配置，随处可用，包括ts、eslint、jest都能读取同一个别名配置；</li>
<li>另外对于webpack的别名配置，网上也有读取tsconfig配置的方案；</li>
</ul>
]]></content>
  </entry>
</search>
